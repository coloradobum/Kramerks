<h1>Listing bookmarks</h1>
<% # link to index action with a paramater flag %>
request.parameters.merge({:sort => "title ASC"})j
<%= link_to 'My Kramerks', bookmarks_path %>

<br/>
<% @kramerks = 'hi'%>

<% if @kramerks %>
  <%= p @kramerks %>
<% end %>

<% if @hashtag %>
  <h2><%= @hashtag %></h2>
  <% @bookmarks.each do |bookmark| %>
    <p><%= link_to bookmark.title, bookmark %></p>
  <% end %>
<% else %>
  <% @hashtags.each do |hashtag| %>
    <h2><%= link_to "##{hashtag.name}", hashtag.name %></h2>
    <% @all_bookmarks[hashtag].each do |bookmark| %>
      <p><%= link_to bookmark.title, bookmark %></p>
    <% end %>
  <% end %>
<% end %>



<%= link_to "New Bookmark", new_bookmark_path %>

